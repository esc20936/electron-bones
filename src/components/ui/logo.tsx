
export function Logo({ className }: { className?: string }) {
  return (
    <div className={`flex items-center justify-center rounded-lg bg-blue-800 p-2 text-white font-black ${className}`}>
      E3
    </div>
  )
}
